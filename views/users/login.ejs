<!DOCTYPE html>
<html>

<%- include('./../partials/head') %>

<title>Login</title>
  <body>
    <%- include('./../partials/header') %>
      <main>
        <h4 class="login">Login</h4>
        <div class=flexLogin>
          <form action="/user/login" method="POST"  class="login">
            <div class="form-group">
              <label for="email">Email</label>
              <input type="email" class="form-control" name="email" placeholder="tumail@proveedor.com">
            </div>
            <div class="form-group">
              <label for="password">Contraseña</label>
              <input type="password" class="form-control" name="password">
            </div>
            <div>
              <% if ( locals.mensaje) {%>
                <p> <%= locals.mensaje %> </p>
              <% }%>
            </div>
            <a href="recupContraseña.html">¿Olvidaste tu contraseña?</a>
            <div>
              <label>
              <input id="inputsChecbox" type="checkbox" name="recordarUsuario">    Recordar Usuario
              </label>
            </div>

            <div class="BotonEnvio">
              <button type="submit" class="btn btn-primary">Submit</button>
            </div>
          </form>
        </div>
      </main>
      <%- include('./../partials/footer') %>
  </body>

</html>